

    /**
     * Create the objects needed to render a generic navigation error page
     * 
     * @param reason 
     *//*
    buildNavigationErrorPage(reason: Error) : IContent {
        let link : ContentLink = {
            guidValue: null,
            id: null,
            providerName: 'Episerver SPA',
            url: EpiContext.Instance.getApplicationPath(),
            workId: null
        }
        let pageData : IContent = {
            changed: null,
            contentLink: link,
            contentType: ['Page','Error','Navigation'],
            created: null,
            existingLanguages: null,
            language: null,
            masterLanguage: null,
            name: reason.name + ": " + reason.message,
            parentLink: null,
            saved: null,
            startPublish: null,
            status: null,
            stopPublish: null
        }
        return pageData;
    }
    */