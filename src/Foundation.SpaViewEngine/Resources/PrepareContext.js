//Prepare a global preload variable
var PreLoad = PreLoad || [];

//Prepare the epi global variable
var epi = epi || {};
epi.isServerSideRendering = true;

//Prepare the gobal rendering function
var render = render || function () { return ""; };